<h1>一覧</h1>
<% @items.each do |item| %>
  <p>欲しいもの</p>
  <span><%= item.name %></span>
  <%= link_to "編集", edit_item_path(item) %>
  <%= link_to "削除", item_path(item), method: :delete , data: {confirm: "削除してもいいですか？"} %>
<% end %>
